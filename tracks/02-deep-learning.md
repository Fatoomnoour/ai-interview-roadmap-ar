# 🧠 Deep Learning

PyTorch, tensors, autograd, optimization, regularization, CNNs, transformers, embeddings, transfer learning, and reproducible experiments.

**Practice:** keep a training log, checkpoint models, inspect failure cases, and report confidence—not only accuracy.
